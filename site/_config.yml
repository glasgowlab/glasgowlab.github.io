#Book settings

title: 
author: 
logo: logo.png

#html:
#  extra_navbar: 

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: cache
  allow_errors: true

only_build_toc_files: true
exclude_patterns: [6_advbiochem.ipynb, 7_LacI_demo.ipynb]

launch_buttons:
  notebook_interface: "jupyterlab"
  thebe: true
  binderhub_url: https://mybinder.org/

# Define the name of the latex output file for PDF builds
#latex:
#  latex_documents:
#    targetname: book.tex

#  Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

sphinx:
  config:
    bibtex_reference_style: author_year

parse:
  myst_enable_extensions:
    # don't forget to list any other extensions you want enabled,
    # including those that are enabled by default!
    - html_image

# Information about where the book exists on the web
repository:
  url: https://glasgowlab.github.io/lab_website/site/1_intro.html  # Online location of your book
  # path_to_book: lab_webpage  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
#html:
#  use_issues_button: true
#  use_repository_button: true
